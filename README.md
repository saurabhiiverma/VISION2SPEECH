# VISION2SPEECH
Vision2Speech is a deep learning-based lip reading system that converts video input into text without using audio. The model combines CNN, Bidirectional LSTM, and CTC loss to learn spatial and temporal features from lip movements. It is trained on the GRID dataset and achieves high accuracy in sentence-level prediction.
